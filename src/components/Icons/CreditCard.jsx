import SVG from 'react-inlinesvg';
import IconCreditCard from 'shared/assets/icons/credit-card.svg';

const CreditCard = (props) => <SVG src={IconCreditCard} {...props} />;

export default CreditCard;
