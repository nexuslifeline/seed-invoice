import SVG from 'react-inlinesvg';
import IconHidePassword from 'shared/assets/icons/hide-password.svg';

const HidePassword = (props) => <SVG src={IconHidePassword} {...props} />;

export default HidePassword;
