import SVG from 'react-inlinesvg';
import IconShowPassword from 'shared/assets/icons/show-password.svg';

const ShowPassword = (props) => <SVG src={IconShowPassword} {...props} />;

export default ShowPassword;
