import SVG from 'react-inlinesvg';
import IconLogo from 'shared/assets/logo/seed-purple-mini.svg';

const LogoPurpleMini = (props) => <SVG src={IconLogo} {...props} />;

export default LogoPurpleMini;
