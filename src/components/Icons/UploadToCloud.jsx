import SVG from 'react-inlinesvg';
import IconUploadToCloud from 'shared/icons/upload-to-cloud.svg';

const UploadToCloud = (props) => <SVG src={IconUploadToCloud} {...props} />;

export default UploadToCloud;
