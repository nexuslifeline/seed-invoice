import SVG from 'react-inlinesvg';
import IconDocument from 'shared/assets/icons/document-file-type.svg';

const Document = (props) => <SVG src={IconDocument} {...props} />;

export default Document;
